version: '3.8'
services:
  searxng:
    image: ${COMPOSE_IMAGE:-searxng:localtest}
    ports:
      - "${PORT:-8080}:8080"
    environment:
      SEARXNG_SETTINGS_PATH: /etc/searxng/settings.yml
    volumes:
      - ./tools/settings.yml:/etc/searxng/settings.yml:ro
    restart: unless-stopped
